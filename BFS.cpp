#include <iostream>
#include <queue>
#include <vector>

using namespace std;

class DataStructure {
};

void BFS(DataStructure &ds, int startVertex) {
}

int main() {
    DataStructure ds;

    int startVertex;
    cout << "Enter the starting vertex: ";
    cin >> startVertex;

    BFS(ds, startVertex);

    return 0;
}
